# Apps

Any apps I'm trying to practice building. Kept separate from the shared
`deployment_tools` team repo so work-in-progress stays private. Each subfolder
is its own standalone app/project.

When something here is ready to share with the team, move it into
`deployment_tools` (or wherever it belongs) and push there instead.

## Projects

- `predium-tech-sanity-check/` -- flags energy source / consumer technology
  combinations worth a second look in Predium exports.

## Note

Real exported `.xlsx` files (Predium exports, customer data) are gitignored
by default -- don't force-add them.
